"use client";

import { useEffect } from "react";
import { Section } from "@/components/ui/Section";

const DemandList = () => {
  useEffect(() => {});

  return (
    <Section title="案件一覧">
      <h1>test</h1>
    </Section>
  );
};

export default DemandList;
